{% extends 'base.html.twig' %}

{% block title %}Hello{% endblock %}

{% block body %}
    <div class="tags-list">
        {% for tag in tags %}
            <a href="#">{{ tag.name }}</a>
        {% endfor %}
    </div><!-- .tags-list -->
{% endblock %}
